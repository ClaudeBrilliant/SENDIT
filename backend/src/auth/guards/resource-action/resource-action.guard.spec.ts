import { ResourceActionGuard } from './resource-action.guard';

describe('ResourceActionGuard', () => {
  it('should be defined', () => {
    expect(new ResourceActionGuard()).toBeDefined();
  });
});
